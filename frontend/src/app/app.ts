import { Component } from '@angular/core';
import { Shell } from './shared/layout/shell';

@Component({
  imports: [Shell],
  selector: 'app-root',
  templateUrl: './app.html',
})
export class App {}
